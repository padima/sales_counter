import 'package:flutter/widgets.dart';

class SalesCounterApp extends StatelessWidget {
  const SalesCounterApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
